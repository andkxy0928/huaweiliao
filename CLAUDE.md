# 花未了项目说明

本目录是已有静态网站的本地修改版，不是 Sites 脚手架。无需 npm 依赖或构建，保持 `file://` 直开首页的兼容性。

## 开发

- 主入口 `index.html`，共用样式 `assets/css/site.css`。
- 资源清单 `assets/js/config.js`，互动实现 `assets/js/app.js`。
- 两个脚本通过 defer 顺序加载，不改为会阻止本地文件运行的模块导入。
- 本地预览：`python scripts/serve.py --port 58743`；未知路径返回自定义 404。
- 静态检查：`node scripts/check.cjs`。
- 禁止擅自推送、公开发布或部署；本地修改与预览由当前任务范围决定。
- 保留 LICENSE 和原项目署名，不将本项目称为开源项目。

## 核心行为

- 保持原有绳系质点物理、触屏锚点抬升、多点触控互斥、手机体感与自动甩。
- 停稳显示 mika1，甩动显示 mika2。
- 原生音频元素随机播放本地 WAV，一次一条，字幕与实际播放同步。
- 不引入旧竹林、3D 小蝉、原始竹知了采样或合成音源。
- 计数与静音偏好仅存本地，不加后端、统计上报或外链服务。
- 修改资源后同步 sw.js 的 CORE 和缓存版本，缓存仅清理本项目命名空间。
- 404 保留用户文案，不能将未知路由缓存为首页。
- 头像、背景等原始图不覆盖；图标派生文件用 scripts/generate-icons.ps1 生成。
- 行为和项目结构变化后同步 README。
